---
phase: 07-system-docker-widgets
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - components/widget-renderer.tsx
  - components/add-widget-dialog.tsx
autonomous: true

must_haves:
  truths:
    - Both system-stats and docker widgets are registered in WidgetRenderer and AddWidgetDialog
    - system-stats type string matches the existing entry in WIDGET_TYPES
    - docker type string is added to WIDGET_TYPES
    - Both widgets receive widgetId and config props from WidgetRenderer
    - Default sizes are appropriate for the content density of each widget
  artifacts:
    - components/widget-renderer.tsx -- updated with system-stats and docker cases
    - components/add-widget-dialog.tsx -- updated with docker entry and default sizes for both
  key_links:
    - WidgetRenderer imports and renders SystemStatsWidget for type "system-stats"
    - WidgetRenderer imports and renders DockerWidget for type "docker"
    - WIDGET_TYPES includes entries for both widgets
    - WIDGET_DEFAULT_SIZES includes sizing for both widgets
---

<objective>
Register the System Stats and Docker widgets in the widget system so they appear in the Add Widget dialog and render correctly on the board.

Purpose: Wire the two new Phase 7 widgets into the existing widget infrastructure, completing the integration.
Output: Updated widget-renderer.tsx and add-widget-dialog.tsx with both new widgets registered.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Existing state after Plans 01 and 02:
- SystemStatsWidget exists at components/widgets/system-stats-widget.tsx
- DockerWidget exists at components/widgets/docker-widget.tsx
- widget-renderer.tsx currently handles: app-links, search, clock, notes, weather, calendar, rss-feed
- add-widget-dialog.tsx WIDGET_TYPES already has a "system-stats" entry but NO "docker" entry
- add-widget-dialog.tsx WIDGET_DEFAULT_SIZES has entries for: search, clock, notes, weather, calendar, rss-feed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register both widgets in WidgetRenderer and AddWidgetDialog</name>
  <files>components/widget-renderer.tsx, components/add-widget-dialog.tsx</files>
  <action>
Update `components/widget-renderer.tsx`:

1. Add imports for both new widgets:
   ```typescript
   import { SystemStatsWidget } from "@/components/widgets/system-stats-widget"
   import { DockerWidget } from "@/components/widgets/docker-widget"
   ```

2. Add two new cases to the switch statement BEFORE the default case:
   ```typescript
   case "system-stats":
     return <SystemStatsWidget widgetId={widgetId} config={config ?? null} />
   case "docker":
     return <DockerWidget widgetId={widgetId} config={config ?? null} />
   ```

Update `components/add-widget-dialog.tsx`:

1. Add the Docker entry to the WIDGET_TYPES array. The "system-stats" entry already exists. Add docker AFTER the system-stats entry:
   ```typescript
   { type: "docker", name: "Docker", description: "Manage Docker containers" },
   ```

2. Add default sizes for both widgets to WIDGET_DEFAULT_SIZES:
   ```typescript
   "system-stats": { w: 3, h: 3 },
   "docker": { w: 4, h: 4 },
   ```
   System stats needs 3x3 for the four-metric grid layout. Docker needs 4x4 for the container list with action buttons.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors.
Verify widget-renderer.tsx has imports and cases for both "system-stats" and "docker".
Verify WIDGET_TYPES in add-widget-dialog.tsx includes both "system-stats" and "docker" entries.
Verify WIDGET_DEFAULT_SIZES includes entries for both "system-stats" and "docker".
Run `npm run build` to confirm the full application builds successfully.
  </verify>
  <done>Both System Stats and Docker widgets are fully registered in the widget system. Adding either widget from the Add Widget dialog creates it with the correct default size and renders the appropriate component. The widget system now supports 10 widget types total.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` completes successfully
3. widget-renderer.tsx imports SystemStatsWidget and DockerWidget
4. widget-renderer.tsx has switch cases for "system-stats" and "docker"
5. WIDGET_TYPES includes entries for "system-stats" (existing) and "docker" (new)
6. WIDGET_DEFAULT_SIZES includes "system-stats": { w: 3, h: 3 } and "docker": { w: 4, h: 4 }
7. Total widget types in the system: app-links, search, clock, notes, weather, system-stats, calendar, rss-feed, docker (9 types)
</verification>

<success_criteria>
- Both widgets appear in the Add Widget dialog with correct names and descriptions
- System Stats widget renders SystemStatsWidget when added to a board
- Docker widget renders DockerWidget when added to a board
- System Stats widget creates at 3x3 default size
- Docker widget creates at 4x4 default size
- Full application builds without errors (`npm run build`)
- No regressions to existing widgets
</success_criteria>

<output>
After completion, create `.planning/phases/07-system-docker-widgets/07-03-SUMMARY.md`
</output>
