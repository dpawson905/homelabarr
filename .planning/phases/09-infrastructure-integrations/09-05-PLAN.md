---
phase: 09-infrastructure-integrations
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - components/widget-renderer.tsx
  - components/add-widget-dialog.tsx
autonomous: true

must_haves:
  truths:
    - All four new widget types are registered in WidgetRenderer with correct imports and switch cases
    - All four new widget types are listed in WIDGET_TYPES in AddWidgetDialog
    - Default sizes are set for all four new widgets in WIDGET_DEFAULT_SIZES
    - Each widget receives widgetId and config props from WidgetRenderer
    - Existing widgets are not affected
  artifacts:
    - components/widget-renderer.tsx -- updated with four new widget cases
    - components/add-widget-dialog.tsx -- updated with four new widget entries and default sizes
  key_links:
    - WidgetRenderer dispatches "dns" to DnsWidget
    - WidgetRenderer dispatches "proxmox" to ProxmoxWidget
    - WidgetRenderer dispatches "home-assistant" to HomeAssistantWidget
    - WidgetRenderer dispatches "uptime-kuma" to UptimeKumaWidget
    - WIDGET_TYPES entries make widgets available in Add Widget dialog
    - WIDGET_DEFAULT_SIZES entries determine initial widget size on the grid
---

<objective>
Register all four Phase 9 widgets (DNS, Proxmox, Home Assistant, Uptime Kuma) in the widget system so they appear in the Add Widget dialog and render correctly on the board.

Purpose: Wire the new infrastructure integration widgets into the existing widget infrastructure, completing Phase 9.
Output: Updated widget-renderer.tsx and add-widget-dialog.tsx with all four new widgets registered.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Components from Plans 01-04:
- DnsWidget at components/widgets/dns-widget.tsx (type: "dns")
- ProxmoxWidget at components/widgets/proxmox-widget.tsx (type: "proxmox")
- HomeAssistantWidget at components/widgets/home-assistant-widget.tsx (type: "home-assistant")
- UptimeKumaWidget at components/widgets/uptime-kuma-widget.tsx (type: "uptime-kuma")

Current widget-renderer.tsx handles these types (13 total):
- app-links, search, clock, notes, weather, calendar, rss-feed, system-stats, docker
- media-server, media-management, download-client, media-requests

Current WIDGET_TYPES in add-widget-dialog.tsx (13 entries):
- app-links, search, clock, weather, system-stats, docker, notes, calendar, rss-feed
- download-client, media-server, media-management, media-requests

Current WIDGET_DEFAULT_SIZES (12 entries):
- search: 4x2, clock: 2x2, notes: 3x3, weather: 3x3, calendar: 3x4, rss-feed: 4x4
- system-stats: 3x3, docker: 4x4, download-client: 4x3, media-server: 4x4
- media-management: 4x4, media-requests: 3x4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register all four widgets in WidgetRenderer and AddWidgetDialog</name>
  <files>components/widget-renderer.tsx, components/add-widget-dialog.tsx</files>
  <action>
**Update `components/widget-renderer.tsx`:**

1. Add four new imports at the top with the existing imports:
   - DnsWidget from "@/components/widgets/dns-widget"
   - ProxmoxWidget from "@/components/widgets/proxmox-widget"
   - HomeAssistantWidget from "@/components/widgets/home-assistant-widget"
   - UptimeKumaWidget from "@/components/widgets/uptime-kuma-widget"

2. Add four new cases to the switch statement BEFORE the default case:
   - "dns" -> DnsWidget with widgetId and config props
   - "proxmox" -> ProxmoxWidget with widgetId and config props
   - "home-assistant" -> HomeAssistantWidget with widgetId and config props
   - "uptime-kuma" -> UptimeKumaWidget with widgetId and config props

**Update `components/add-widget-dialog.tsx`:**

1. Add four new entries to the WIDGET_TYPES array after the existing media entries:
   - { type: "dns", name: "DNS/Ad-Block", description: "DNS stats from Pi-hole or AdGuard Home" }
   - { type: "proxmox", name: "Proxmox", description: "VM and container status from Proxmox VE" }
   - { type: "home-assistant", name: "Home Assistant", description: "Smart home entity states and controls" }
   - { type: "uptime-kuma", name: "Uptime Kuma", description: "Service monitor status and uptime" }

2. Add four new entries to WIDGET_DEFAULT_SIZES:
   - "dns": { w: 3, h: 3 } — compact stats grid with top clients list
   - "proxmox": { w: 4, h: 4 } — needs space for node + guest lists
   - "home-assistant": { w: 3, h: 4 } — entity list with toggle controls
   - "uptime-kuma": { w: 3, h: 4 } — monitor list with uptime bars
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors.
Verify widget-renderer.tsx has imports and cases for all four new types.
Verify WIDGET_TYPES has 17 entries total (13 existing + 4 new).
Verify WIDGET_DEFAULT_SIZES has 16 entries total (12 existing + 4 new).
Run `npm run build` to confirm the full application builds successfully.
  </verify>
  <done>All four Phase 9 widgets registered in the widget system. DNS, Proxmox, Home Assistant, and Uptime Kuma widgets appear in the Add Widget dialog with correct names, descriptions, and default sizes. WidgetRenderer dispatches each type to the correct component. Total widget types: 17.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` completes successfully
3. widget-renderer.tsx imports all four new widget components
4. widget-renderer.tsx has switch cases for "dns", "proxmox", "home-assistant", "uptime-kuma"
5. WIDGET_TYPES in add-widget-dialog.tsx has 17 entries (13 existing + 4 new)
6. WIDGET_DEFAULT_SIZES has 16 entries (12 existing + 4 new)
7. All four new widgets receive widgetId and config props
8. No regressions to existing 13 widget types
</verification>

<success_criteria>
- All four new widgets appear in the Add Widget dialog with correct names and descriptions
- DNS widget renders DnsWidget at 3x3 default size
- Proxmox widget renders ProxmoxWidget at 4x4 default size
- Home Assistant widget renders HomeAssistantWidget at 3x4 default size
- Uptime Kuma widget renders UptimeKumaWidget at 3x4 default size
- Full application builds without errors (`npm run build`)
- No regressions to existing widgets
- Widget system now supports 17 widget types total
</success_criteria>

<output>
After completion, create `.planning/phases/09-infrastructure-integrations/09-05-SUMMARY.md`
</output>
